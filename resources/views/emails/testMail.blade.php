<!DOCTYPE html>
<html>
<head>
    <title>Speiseplan</title>
</head>
<body>
    <h1>{{ $testMailData['title'] }}</h1>
    <p>{{ $testMailData['body'] }}</p>
    <p>{{ $testMailData['user'] }}</p>
</body>
</html>